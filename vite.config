
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react()],
  build: {
    // Aumenta o limite de aviso para 1600kb para evitar spam no log
    chunkSizeWarningLimit: 1600,
    rollupOptions: {
      output: {
        // Divide bibliotecas pesadas (vendor) do código principal
        manualChunks(id) {
          if (id.includes('node_modules')) {
            return 'vendor';
          }
        },
      },
    },
  },
});
